{
  "name": "get-value-in-range",
  "version": "1.0.9",
  "description": "Calculate range adjusted values",
  "repository": "flipactual/get-value-in-range",
  "author": {
    "name": "Flip",
    "id": 1306968,
    "url": "https://github.com/flipactual"
  },
  "main": "lib/index.js",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "test": "mocha",
    "coverage": "nyc --reporter=html mocha",
    "view-coverage": "open ./coverage/index.html",
    "codecov": "nyc --reporter=text-lcov mocha > coverage.lcov && codecov",
    "lint": "eslint ./**/*.js --",
    "readme": "emdaer",
    "compile": "babel src -d lib",
    "prepublish": "npm run compile"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "codecov": "^1.0.1",
    "emdaer": "^0.3.2",
    "eslint": "^3.2.2",
    "eslint-config-airbnb-base": "^5.0.1",
    "eslint-plugin-import": "^1.13.0",
    "mocha": "^2.3.4",
    "nyc": "^10.0.0"
  }
}
