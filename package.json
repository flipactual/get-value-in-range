{
  "name": "get-value-in-range",
  "version": "1.0.0",
  "description": "Find the position of the value within a range.",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/_mocha --",
    "lint": "node_modules/.bin/eslint ./**/*.js --"
  },
  "repository": "flipactual/get-value-in-range",
  "author": "Flip",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.2.2",
    "eslint-config-airbnb-base": "^5.0.1",
    "eslint-plugin-import": "^1.12.0",
    "mocha": "^2.3.4"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}
